import { Text, View } from 'react-native';

export default function GameScreen() {
  return (
    <View className="flex h-full items-center justify-center">
      <Text className="text-3xl">Game Screen</Text>
    </View>
  );
}
